

const initialState = {
	isLoading: false,
	isLoggedIn: true,
	itemData: [],
	userData: {},
	newItemData: {},
	error: ''
}

const reducer = (state = initialState, action) => {
    switch (action.type) {

    }
        
}


export default reducer;